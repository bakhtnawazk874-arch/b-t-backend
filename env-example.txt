# Copy this file to .env and fill in your real values. Never commit .env to git.

# Your own PostgreSQL database (run schema.sql on it first)
DATABASE_URL=postgresql://username:password@localhost:5432/bt_ai

# Your own Anthropic API key — https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

PORT=3000
